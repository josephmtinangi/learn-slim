{% extends 'layouts/app.twig' %}

{% block content %}

    <div class="container">
        <div class="row">
            <div class="col-sm-12">
                <h1>{{ project.name }}</h1>
                <p>
                    {{ project.description }}
                </p>
            </div>
        </div>
    </div>

{% endblock %}